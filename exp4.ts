// function soma(num1: number, num2: number){
//     return num1 + num2
// }

// const soma = (num1: number, num2: number) => {
//     return num1 + num2
// }

// console.log(soma(520, 5));

// function nomeCompleto(nome: string, sobrenome: string): string {
//     return `${nome} ${sobrenome}`
// }

const completo = (nome: string, sobrenome: string): string => {
    return `${nome} ${sobrenome}`
}

console.log(completo("Caio", "Monteiro"));
